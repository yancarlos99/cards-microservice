spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: ms-eureka-user
      password: admin

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false